// This module is no longer used. Angular 16+ supports standalone APIs and Angular 20 encourages module-less apps.
// Keeping the file to avoid breaking imports during migration; remove once all references are updated.
export {};